import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // colores
  // fuentes
  // espacios
  // sombres
  palette: {
    primary: {
      light: "#63a4ff",
      main: "#1976d2",
      dark: "#004ba0",
    },
    secondary: {
      light: "#ff616f",
      main: "#ff1744",
      dark: "#c4001d",
    },
  },
});

export default theme;
